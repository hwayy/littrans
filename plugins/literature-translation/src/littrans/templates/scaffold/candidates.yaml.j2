# Candidate terminology: the record of promotion decisions. Nothing here gates or reaches
# a packet. An entry without `status` (or `status: proposed`) is undecided and is listed in
# the finalize "unresolved decisions" report; an entry whose status records a decision
# (`reference-only`, `rejected`, ...) is only counted there. Keep the measurement that led to
# each decision in `definition` so the next round does not repeat it.
terms: []
