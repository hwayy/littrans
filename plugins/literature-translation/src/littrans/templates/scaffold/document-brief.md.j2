# Document brief

> **Rules only.** This file is injected verbatim into every translate and audit packet and
> hashed **whole** into the audit context: one changed line marks every finished audit
> `context-changed`. Put here what is true of the whole document and will not grow with
> chapters. Anything that grows as chapters are prepared (proper names, ambiguous words,
> per-chapter notes) belongs in `glossary/reference.yaml`, which packets receive filtered to
> the units that mention it. Before editing, ask: did a rule change, or was a record added?

Complete this brief before translating: subject, argument, audience, terminology, and source style.
